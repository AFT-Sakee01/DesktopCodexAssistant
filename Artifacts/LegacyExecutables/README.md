# Legacy Executables

This folder keeps historical local executable builds that were previously mixed into the project root.

The active release binary is `DesktopCodexAssistant.exe` in the project root. Files here are retained only for traceability and are named by product, build timestamp, and a short SHA1 prefix.
